# 51 单片机基础练手题库

> 用途：通过循序渐进的小题，熟悉 GPIO、按键、LED、蜂鸣器、定时器、中断、PWM、串口、ADC、数码管、I2C 等外设，并练习多个外设之间的配合。  
> 适用工程：`51_Project`  适用芯片：STC8G/STC8H 系列 8051  
> 建议方式：严格按照顺序练习；每完成一道题，勾选题目前的复选框，并记录实际现象。

## 使用规则

1. 每道题先写出自己的思路，再开始写代码。
2. 先确认初始化函数、引脚电平有效逻辑和模块头文件，再编写业务流程。
3. 涉及定时器的题目，优先使用 `system_ms` 和时间差判断，不使用长时间阻塞延时。
4. 中断服务函数只做快速、必要的工作；LED、蜂鸣器、串口解析等业务放在主循环任务中。
5. 新增模块时，分别创建 `.h` 和 `.c`，并把 `.c` 文件加入 Keil 当前 Target。
6. 每道题完成后至少检查：上电初始状态、正常操作、重复操作、边界情况和异常输入。
7. 本题库中的“建议文件”不是唯一答案，可以根据当前工程结构合理拆分。

## 代码位置约定

- 标注“**新建 Lab**”的题目：在 `51_Project/src/Labs/` 下新建同名文件夹，并创建同名 `.c`、`.h`；完成后在 `main.c` 中只启用当前 Lab 的 `Init()` 和 `Task()`，再运行 `tools/sync_labs_to_keil.sh <Lab名称>` 同步到 Keil。
- 标注“**复用/扩展现有文件**”的题目：直接在指定文件中继续写，不新建文件夹；每次练习只在 `main.c` 启用一个主实验入口，避免多个实验同时控制同一外设。
- 标注“**修改驱动模块**”的题目：在模块 `.c` 实现通用硬件能力，在 `.h` 声明公共接口；Lab 只调用接口，不直接操作该模块的 GPIO、PWM 或寄存器。

## 当前硬件约定

- 主频：24 MHz。
- 按键：当前练习主要使用 KEY1=P5.1、KEY2=P5.2，带上拉输入，低电平表示按下。
- LED：低电平点亮，高电平熄灭；`LED_SetAll(1)` 表示点亮全部 LED，`LED_SetAll(0)` 表示熄灭全部 LED。
- 蜂鸣器：通过 `Buzzer_Beep()` 开始输出，通过 `Buzzer_Stop()` 停止输出。
- 系统时间：Timer0 每 1 ms 产生一次中断，`system_ms` 为 `u16` 类型并由 ISR 更新。
- 常用模块：`LED`、`Key`、`Buzzer`、`Timers`、`Motor`、`PWM`、`UART`、`ADC`、`NTC`、`Nixie`、`I2C`。

## 第一阶段：GPIO、LED 和按键基础

- [x] **题目 01：LED 初始化与全亮全灭**（基础）

  **目标**：熟悉 GPIO 输出模式、LED 的初始化和低电平点亮逻辑。

  **要求**：

  - 创建或完善一个 LED 测试任务。
  - 上电后所有 LED 必须保持熄灭，不能出现随机亮灯。
  - 按照“全灭 1 秒、全亮 1 秒、全灭 1 秒”的顺序循环运行。
  - 必须先调用 `LED_Init()`，再调用 LED 控制函数。
  - 尽量通过 `LED_SetAll()` 控制全部 LED，不要在业务代码中重复操作每个引脚。

  **验收标准**：8 个 LED 状态一致，亮灭逻辑与代码注释一致；复位后立即处于确定的熄灭状态。

  **建议文件**：`src/Labs/LedBasicLab/LedBasicLab.c`。

  **本题编写位置**：复用现有 `LedBasicLab`；主要写 `src/Labs/LedBasicLab/LedBasicLab.c`，如需对外声明 `Init()`、`Task()` 再同步修改同目录 `.h`。不需要新建文件夹。

- [x] **题目 02：指定编号 LED 独立点亮**（基础）

  **目标**：练习数组下标、`switch` 映射和“先清除旧状态，再设置新状态”。

  **要求**：

  - 编写 `LED_ShowOnly(u8 index)`。
  - `index=0` 到 `index=7` 分别只点亮一盏对应 LED。
  - 调用函数时，必须先熄灭全部 LED，防止上一次点亮的 LED 残留。
  - 传入非法编号时，所有 LED 保持熄灭。
  - 在头文件中说明编号和实际 LED 的对应关系。

  **验收标准**：连续调用 0、1、2……7 时始终只有一盏 LED 亮；非法输入不会访问数组边界。

  **建议文件**：完善 `src/LED/LED.c` 和 `src/LED/LED.h`。

  **本题编写位置**：修改驱动模块；在 `src/LED/LED.c` 实现 `LED_ShowOnly()`，在 `src/LED/LED.h` 添加函数声明。用 `LedBasicLab.c` 或一个临时测试调用验证，不在 Lab 中直接操作 LED 引脚。

- [x] **题目 03：按键控制 LED 开关**（基础）

  **目标**：区分按键当前电平和业务动作。

  **要求**：

  - KEY1 按下时点亮全部 LED，松开时熄灭全部 LED。
  - KEY2 按下时只点亮 LED1，松开时熄灭全部 LED。
  - 初始化时必须调用 `Key_Init()`，确认按键配置为带上拉输入。
  - 在代码注释中写清楚“松开为 1、按下为 0”的原因。
  - 暂时允许使用简单延时消抖，但要说明延时对主循环响应的影响。

  **验收标准**：两个按键能够独立控制对应输出，不按键时 LED 不自行变化。

  **建议文件**：`src/Labs/KeyLedLab/KeyLedLab.c`。

  **本题编写位置**：复用现有 `KeyLedLab`；主要写 `src/Labs/KeyLedLab/KeyLedLab.c`，并调用既有的 `Key`、`LED` 模块接口。不需要新建文件夹。

- [x] **题目 04：按下一次只翻转一次 LED**（基础）

  **目标**：练习按键边沿检测和“按住不重复触发”。

  **要求**：

  - 每按下一次 KEY1，只翻转一次 LED 全亮/全灭状态。
  - 按住 KEY1 不允许持续翻转。
  - 必须保存上一次按键状态。
  - 只有检测到“松开 → 按下”变化时才执行翻转。
  - 松开后再次按下，才能产生下一次动作。

  **验收标准**：快速观察时 LED 不会因为按住按键而反复闪烁；每次完整按下动作只触发一次。

  **本题编写位置**：复用 `src/Labs/LedFlipLab/LedFlipLab.c`；在这里保存按键状态和 LED 状态，实现“按下一次翻转一次”。不修改 `Key.c` 或 `LED.c`。

- [x] **题目 05：非阻塞按键消抖**（基础）

  **目标**：理解机械抖动，并使用系统时间替代 `delay_ms()` 消抖。

  **要求**：

  - 保存每个按键的原始状态、稳定状态、最近变化时间。
  - 原始电平变化时，只记录变化，不立即产生业务事件。
  - 原始电平连续保持 10 ms 后，才更新稳定状态。
  - 稳定状态从松开变为按下时，产生一次 `key_press_event`。
  - 业务处理完成后清除事件，确保按住按键不会重复启动。
  - 消抖期间主循环仍然可以执行 LED 或其他任务。

  **验收标准**：按键抖动不会产生多次动作；主循环没有长时间停在延时函数中。

  **建议参考**：`src/Labs/BuzzerSequenceLab/BuzzerSequenceLab.c` 中的按键事件处理方式。

  **本题编写位置**：继续扩展 `src/Labs/LedFlipLab/LedFlipLab.c`，将第四题的简单按键处理替换为非阻塞消抖逻辑；不需要另建文件夹。`BuzzerSequenceLab.c` 仅用于参考其消抖思路。

## 第二阶段：蜂鸣器与简单外设配合

- [x] **题目 06：蜂鸣器单音播放**（基础）

  **目标**：熟悉蜂鸣器初始化、启动和停止接口。

  **要求**：

  - 上电后蜂鸣器必须停止。
  - KEY1 按下后播放一个指定音调。
  - KEY1 松开后停止蜂鸣器。
  - 重复按下和松开时，蜂鸣器状态必须正确恢复。
  - 不允许直接在业务代码中操作蜂鸣器底层寄存器，应调用模块公开接口。

  **验收标准**：按键按住才有声音，松开立即停止；上电不会自动发声。

  **本题编写位置**：新建 Lab：`src/Labs/BuzzerToneLab/BuzzerToneLab.c` 和 `.h`。Lab 只调用 `Buzzer_Init()`、`Buzzer_Beep()`、`Buzzer_Stop()` 与 `Key_IsPressed()`；不修改蜂鸣器底层 GPIO/PWM 配置。

- [x] **题目 07：LED 与蜂鸣器同步报警**（基础）

  **目标**：练习一个输入同时控制多个外设。

  **要求**：

  - KEY1 按下时 LED 全亮并启动蜂鸣器。
  - KEY1 松开时 LED 全灭并停止蜂鸣器。
  - 将“开始报警”和“停止报警”分别封装成函数。
  - 停止函数必须同时关闭所有相关外设。
  - 检查如果 LED 已经亮着或蜂鸣器已经停止，重复调用停止函数是否安全。

  **验收标准**：LED 和蜂鸣器状态始终一致，不出现 LED 已灭但蜂鸣器仍响的情况。

  **本题编写位置**：新建 Lab：`src/Labs/AlarmLab/AlarmLab.c` 和 `.h`。在此封装 `Alarm_Start()`、`Alarm_Stop()` 与 `AlarmLab_Task()`；调用 `LED`、`Buzzer`、`Key` 的公开接口，不修改它们的驱动文件。

- [x] **题目 08：音符数组顺序播放**（基础）

  **目标**：练习数组、下标和播放流程控制。

  **要求**：

  - 建立一个至少包含 3 个音符的数组，例如 `C5、D5、E5`。
  - KEY1 按下后从数组下标 0 开始播放。
  - 当前音符播放结束后，下标加 1，播放下一个音符。
  - 播放到最后一个音符后，关闭蜂鸣器并把下标恢复为 0。
  - 播放结束后再次按 KEY1，必须从第一个音符重新开始。
  - 明确处理 `note_index >= NOTE_COUNT` 的边界。

  **验收标准**：音符顺序正确，数组不会越界，播放完成后状态能够复位。

  **本题编写位置**：新建基础版 Lab：`src/Labs/BuzzerArrayLab/BuzzerArrayLab.c` 和 `.h`。音符数组、下标、播放函数和结束复位逻辑都写在该 Lab 中；不把旋律业务逻辑写进 `src/Buzzer/Buzzer.c`。已有的 `BuzzerSequenceLab` 留给后续非阻塞状态机练习，不要覆盖。

- [x] **题目 09：按键中止阻塞式旋律**（基础）

  **目标**：观察阻塞延时对按键响应的影响。

  **要求**：

  - 在每个音符播放期间加入一段总时长较长的等待。
  - 等待期间分成 10 ms 小段，每段检查 KEY2。
  - KEY2 有效按下后立即停止蜂鸣器、关闭 LED、复位播放位置。
  - 对比“一个长延时”和“分段检查按键”两种方案的响应差异。
  - 在总结中写出这种方案仍然有哪些局限。

  **验收标准**：按下 KEY2 后能够停止，但能够说明它仍然会占用主循环时间。

  **本题编写位置**：继续扩展 `src/Labs/BuzzerArrayLab/BuzzerArrayLab.c`，新增“分段等待并检查 KEY2”的阻塞式版本。它是与第八题对比的实验，不需要新建文件夹；`BuzzerSequenceLab` 仍保留给后续非阻塞状态机版本。

## 第三阶段：定时器、中断和系统节拍

- [x] **题目 10：计算并配置 1 ms 定时器**（基础）

  **目标**：理解定时器时钟、计数次数和自动重装初值。

  **要求**：

  - 使用 Timer0，配置为 16 位自动重装模式。
  - 主频按 24 MHz、定时器 1T 模式计算。
  - 计算 1 ms 需要的计数次数。
  - 计算 16 位定时器的初值，并写在代码注释中。
  - 让 ISR 每次只执行 `system_ms++`。
  - `system_ms` 必须使用 `volatile u16`。

  **验收标准**：能够解释 24 MHz、1000 Hz、24000 次计数和重装值之间的关系。

  **建议文件**：`src/Timer/Timers.c`、`drivers/Timer_Isr.c`。

  **本题编写位置**：修改定时器模块；在 `src/Timer/Timers.c` 配置 Timer0，在 `drivers/Timer_Isr.c` 中仅递增 `system_ms`，并在 `src/Timer/Timers.h` 提供读取系统时间的接口。不要将定时器初始化代码写入某个 Lab。

- [x] **题目 11：使用 system_ms 让 LED 每 300 ms 翻转**（基础）

  **目标**：掌握非阻塞周期任务的标准写法。

  **要求**：

  - 在任务函数中读取当前系统时间。
  - 使用 `now - led_last_ms >= 300` 判断是否到期。
  - 到期后先更新时间记录，再翻转 LED 状态。
  - 不允许在任务中使用 `delay_ms(300)`。
  - 解释为什么不能使用 `now >= led_last_ms` 表示经过了 300 ms。

  **验收标准**：LED 约每 300 ms 改变一次状态，同时主循环仍能响应按键。

  **本题编写位置**：复用 `src/Labs/TimerTickLab/TimerTickLab.c`；在任务函数中使用 `Timers_GetSystemMs()` 完成时间差判断。不要修改 ISR 来实现 LED 闪烁。

- [x] **题目 12：使用 system_ms 实现蜂鸣器周期短响**（基础）

  **目标**：使用多个时间点管理一个具有开始和结束阶段的动作。

  **要求**：

  - 蜂鸣器停止时等待 700 ms 后启动。
  - 每次启动蜂鸣器时记录 `buzzer_start_ms`。
  - 蜂鸣器持续 100 ms 后停止，并把 `buzzer_active` 置 0。
  - 700 ms 的启动判断和 100 ms 的停止判断必须是两个独立的 `if`。
  - 初始状态必须为停止，首次动作必须等待完整的 700 ms。

  **验收标准**：蜂鸣器呈现“等待 700 ms、响 100 ms、继续等待”的周期效果。

  **本题编写位置**：继续扩展 `src/Labs/TimerTickLab/TimerTickLab.c`，与 LED 300 ms 任务并列编写蜂鸣器的两个时间判断；不需要新建文件夹。

- [x] **题目 13：验证 system_ms 回绕安全性**（提高）

  **目标**：理解无符号计数器溢出后的时间差计算。

  **要求**：

  - 将 `system_ms` 或测试时间变量设置到接近 `65535` 的位置。
  - 模拟时间从 `65535` 回到 `0` 的过程。
  - 验证 `now - last_ms >= interval` 在回绕前后仍然能够工作。
  - 写出该方法成立的前提：任务间隔必须远小于计数器的最大范围。
  - 比较直接使用绝对时间判断和使用时间差判断的区别。

  **验收标准**：能够用具体数字说明回绕前后的差值结果。

  **本题编写位置**：新建 `src/Labs/TimerWrapLab/TimerWrapLab.c` 和 `.h`，用单独的测试时间变量演示回绕；不要为了测试而修改正在运行的全局 `system_ms` 或定时器 ISR。

## 第四阶段：非阻塞状态机

- [x] **题目 14：画出蜂鸣器旋律状态转换图**（基础）

  **目标**：在写代码前先建立状态机思维。

  **要求**：

  - 画出 `IDLE`、`NOTE`、`GAP` 三个状态。
  - 标注 K1 启动、K2 停止、音符 300 ms 到期、间隔 50 ms 到期等转换条件。
  - 标明每个状态进入时要执行的动作。
  - 标明最后一个音符播放完成后的回到空闲路径。
  - 说明为什么 K2 在 `NOTE` 和 `GAP` 两个状态都必须可以停止。

  **验收标准**：不看代码也能根据流程图讲清楚旋律播放过程。

  **本题编写位置**：先不改 C 代码。新建说明文件 `src/Labs/BuzzerSequenceLab/状态机图.md`，在其中画出或用文字表示状态转换；后续第十五题再修改同目录的 `BuzzerSequenceLab.c`。

- [x] **题目 15：将 PlaySequence 改为非阻塞状态机**（基础）

  **目标**：完成从阻塞式播放到时间驱动状态机的转换。

  **要求**：

  - 使用 `SEQUENCE_IDLE`、`SEQUENCE_NOTE`、`SEQUENCE_GAP`。
  - `NOTE` 状态持续 300 ms，结束后停止声音并进入 `GAP`。
  - `GAP` 状态持续 50 ms，结束后播放下一个音符。
  - 每次调用播放任务只允许推进有限的一步，然后立即返回。
  - K1 只在空闲状态启动旋律，播放期间再次按 K1 不得重置当前进度。
  - 播放结束或 K2 停止后，蜂鸣器关闭、LED 熄灭、下标清零、状态恢复为空闲。

  **验收标准**：播放期间按 K2 可以及时停止；主循环仍可以执行其他任务。

  **建议参考**：`src/Labs/BuzzerSequenceLab/BuzzerSequenceLab.c`。

  **本题编写位置**：复用并修改 `src/Labs/BuzzerSequenceLab/BuzzerSequenceLab.c`；将原先的阻塞式播放流程改为 `IDLE/NOTE/GAP` 状态机。状态变量和时间变量属于该 Lab，不应放进 `Buzzer.c`。

- [x] **题目 16：增加暂停与继续功能**（提高）

  **目标**：练习在现有状态机上增加新的业务状态。

  **要求**：

  - 使用 KEY2 作为暂停/继续键，另选一个按键作为停止键。
  - 暂停时必须停止蜂鸣器，但保存当前音符下标和已经播放的时间。
  - 继续时不能重新从当前音符的 0 ms 开始，除非你明确设计为重新播放当前音符。
  - 暂停期间不允许自动推进音符和间隔时间。
  - 停止后必须清理全部状态，下一次从第一个音符开始。

  **验收标准**：暂停期间 LED 可以显示暂停状态，继续后播放流程符合设计，停止后能够完全复位。

  **本题编写位置**：继续扩展 `src/Labs/BuzzerSequenceLab/BuzzerSequenceLab.c`，在第十五题的状态机上增加暂停相关状态与变量；不新建第二套旋律代码。

## 第五阶段：PWM、LED 亮度和马达控制

- [x] **题目 17：固定档位 LED PWM 亮度**（基础）

  **目标**：熟悉 PWM 占空比和亮度之间的关系。

  **要求**：

  - 建立 0%、25%、50%、75%、100% 五个亮度档位。
  - 使用按键切换档位，不能直接用延时模拟亮度。
  - 对输入值进行 0~100% 范围限制。
  - 在代码中说明占空比与 LED 实际亮度的关系，以及低电平点亮可能带来的逻辑反向。
  - 检查重复初始化 PWM 是否会影响当前输出。

  **验收标准**：各档亮度有明显区别，输入超过 100% 时不会造成异常占空比。

  **本题编写位置**：新建 `src/Labs/LedPwmLab/LedPwmLab.c` 和 `.h`；Lab 调用现有 `LED_Init()`、`LED_SetBrightness()` 和按键接口。PWM 的寄存器配置继续保留在 LED/PWM 模块中。

- [x] **题目 18：电位器控制马达速度**（基础）

  **目标**：练习 ADC 采样、数值映射和 PWM 输出的组合。

  **要求**：

  - 初始化 ADC 输入和马达 PWM 输出。
  - 读取 ADC 值并将 ADC 范围映射到 0~100%。
  - 通过 `Motor_SetSpeed()` 设置马达速度，不在 ADC 模块中直接操作马达寄存器。
  - 电位器最小时马达停止，最大时达到最高设定速度。
  - 对 ADC 结果和映射结果进行边界限制。

  **验收标准**：旋转电位器时马达速度平滑变化，断电或复位后马达不会自动高速运行。

  **建议参考**：`src/ADC/ADCS.c`、`src/Motor/Motor.c`。

  **本题编写位置**：新建 `src/Labs/PotMotorLab/PotMotorLab.c` 和 `.h`；该 Lab 读取 ADC 模块公开接口并调用 `Motor_SetSpeed()`。若公共接口缺失，再分别在 `ADCS.c/.h`、`Motor.c/.h` 补充，不能在 Lab 直接写 ADC/PWM 寄存器。

- [ ] **题目 19：按键选择 LED/马达控制模式**（提高）

  **目标**：练习同一个输入数据被不同业务模式使用。

  **要求**：

  - KEY1 切换“LED 亮度模式”和“马达速度模式”。
  - 一个模式下 ADC 控制 LED 亮度，另一个模式下 ADC 控制马达速度。
  - LED 和马达模块保持独立，模式模块只负责选择调用谁。
  - 使用状态变量保存当前模式，不能通过多个互相冲突的布尔变量表达同一状态。
  - 切换模式时，对不工作的外设执行明确的安全处理。

  **验收标准**：模式切换可靠，当前模式显示清晰，不会出现两个模块同时争用同一 PWM 资源。

  **本题编写位置**：新建 `src/Labs/AdcModeLab/AdcModeLab.c` 和 `.h`，在该 Lab 保存模式变量并分发 ADC 数据给 LED 或马达模块；不把模式判断分散写进 ADC、LED、Motor 驱动中。

## 第六阶段：串口通信

- [ ] **题目 20：串口回显**（基础）

  **目标**：熟悉 UART 初始化、接收缓冲区和发送数据。

  **要求**：

  - 初始化 UART1，设置一个明确的波特率。
  - 上位机发送一个字节后，单片机原样返回该字节。
  - 接收和发送代码分别封装，不把所有逻辑写进 `main.c`。
  - 处理连续发送多个字节的情况。
  - 如果使用接收超时判断一条消息结束，要在注释中说明超时含义。

  **验收标准**：串口助手发送的数据能够稳定回显，乱码时能够从波特率、串口引脚和时钟配置排查。

  **本题编写位置**：复用 `src/UART/UARTS.c` 和 `.h`，完成初始化后的接收、回显和消息处理；`drivers/UART.c`、`drivers/UART_Isr.c` 保持为底层驱动。`main.c` 只调用 `UART_Init()` 和消息任务函数。

- [ ] **题目 21：串口命令控制 LED**（基础）

  **目标**：练习串口数据解析和外设接口调用。

  **要求**：

  - 发送字符 `0` 关闭全部 LED，发送字符 `1` 点亮全部 LED。
  - 同时兼容原始字节 `0x00` 和 `0x01`，并说明字符 `'0'` 与数值 `0` 的区别。
  - 串口业务层只能调用 `LED_SetAll()`，不能直接修改 LED 引脚。
  - 处理未知命令并通过串口返回错误提示。
  - 命令处理完成后清理接收缓冲区状态。

  **验收标准**：合法命令控制正确，非法命令不会改变 LED 状态。

  **本题编写位置**：继续扩展 `src/UART/UARTS.c` 中的命令解析函数，并通过 `LED_SetAll()` 控制 LED；需要公开新命令处理接口时同步修改 `UARTS.h`。不新建单独的 LED 驱动代码。

- [ ] **题目 22：串口设置 LED 亮度和马达速度**（提高）

  **目标**：练习带参数命令的解析和多模块协同。

  **要求**：

  - 设计至少两种命令，例如 `LED:60`、`MOTOR:30`。
  - 解析命令类型和 0~100 的参数。
  - 参数缺失、包含非数字字符或超过范围时，返回明确错误。
  - 解析模块只负责得到命令和参数，真正控制由 LED/PWM/Motor 模块完成。
  - 处理一条命令后，必须等待下一条完整命令，不能重复执行旧数据。

  **验收标准**：串口命令能够控制两个不同外设，错误输入不会导致占空比越界或程序卡死。

  **本题编写位置**：继续在 `src/UART/UARTS.c` 中实现字符串与参数解析；亮度和速度的实际设置分别调用 LED、Motor 模块接口。若解析代码明显变长，可新建 `src/UART/UartCommand.c/.h` 专门负责解析。

## 第七阶段：ADC、NTC 和数码管

- [ ] **题目 23：ADC 数值采集与数码管显示**（基础）

  **目标**：建立“采样 → 处理 → 显示”的数据链路。

  **要求**：

  - 初始化 ADC 和数码管。
  - 周期读取 ADC 原始值。
  - 将原始值拆分成百位、十位和个位。
  - 在数码管上稳定显示结果，不能因为主循环中有其他任务而完全不刷新。
  - 对 ADC 最大值和最小值分别测试。

  **验收标准**：输入变化时显示值随之变化，显示位置和数字顺序正确。

  **本题编写位置**：新建 `src/Labs/AdcNixieLab/AdcNixieLab.c` 和 `.h`；Lab 调用 ADC 采样与数码管显示模块的公共接口，采样和显示驱动本身不写在 Lab 内。

- [ ] **题目 24：NTC 温度显示**（基础）

  **目标**：熟悉传感器数据转换和周期性显示。

  **要求**：

  - 定期读取 NTC 温度。
  - 通过数码管显示温度的整数部分，明确是否显示正负号。
  - 采样任务和数码管刷新任务分开，不让一次采样阻塞全部显示。
  - 设计传感器读取失败或数据异常时的显示方式。
  - 通过串口输出温度，方便对比数码管结果。

  **验收标准**：温度变化时显示和串口输出基本一致，异常值不会造成数组越界。

  **本题编写位置**：新建 `src/Labs/NtcDisplayLab/NtcDisplayLab.c` 和 `.h`；温度读取使用 NTC 模块，显示使用 Nixie 模块，串口输出使用 UART 模块，Lab 只负责周期调度与异常显示策略。

- [ ] **题目 25：温度阈值报警**（提高）

  **目标**：练习传感器、定时器、LED、蜂鸣器和串口的综合配合。

  **要求**：

  - 上电后记录一个基准温度，或设置一个固定报警阈值。
  - 温度超过阈值时启动 LED 闪烁和蜂鸣器间歇报警。
  - 温度恢复正常后关闭蜂鸣器并熄灭 LED。
  - 报警逻辑必须使用非阻塞时间任务，不能用一个长延时卡住整个程序。
  - 增加滞回区，例如超过阈值加 2℃才报警，低于阈值减 1℃才解除，避免临界点反复切换。
  - 串口输出当前温度、报警状态和阈值。

  **验收标准**：温度跨过阈值时报警状态变化正确，临界温度附近不会快速抖动。

  **本题编写位置**：新建 `src/Labs/TempAlarmLab/TempAlarmLab.c` 和 `.h`；在 Lab 中保存阈值、滞回、报警状态和时间变量，调用 NTC、LED、Buzzer、UART 的公开接口。

## 第八阶段：I2C 和 RTC

- [ ] **题目 26：I2C 读取 RTC 时间**（提高）

  **目标**：熟悉 I2C 初始化、设备地址、寄存器地址和 BCD 数据转换。

  **要求**：

  - 初始化 I2C、相关 GPIO、串口和必要的外部中断模块。
  - 从 RTC 读取秒、分、时、日、周、月、年。
  - 将 BCD 数据转换为十进制后通过串口输出。
  - 明确区分设备地址和寄存器地址。
  - 检查 I2C 源文件及其依赖源文件是否都加入 Keil Target。
  - 读取失败时输出错误提示，不要继续使用未初始化的数据。

  **验收标准**：串口能够稳定输出格式正确的时间，时间每秒正常变化。

  **建议参考**：`src/I2C/IIC.c`、`src/I2C/IIC.h`。

  **本题编写位置**：新建 `src/Labs/RtcReadLab/RtcReadLab.c` 和 `.h`，在其中组织“读取 RTC → BCD 转十进制 → 串口输出”的业务。若 I2C/RTC 的通用读写接口缺失，再补充 `src/I2C/IIC.c/.h`，不要在 Lab 直接操作 I2C 寄存器。

- [ ] **题目 27：设置 RTC 时间**（提高）

  **目标**：练习 I2C 写入多个连续寄存器。

  **要求**：

  - 设计一组待写入的年月日、星期、时分秒数据。
  - 将十进制时间转换为 RTC 所需的 BCD 格式。
  - 一次写入连续的时间寄存器。
  - 写入后重新读取并通过串口验证。
  - 对月份、日期、小时、分钟和秒进行基本范围检查。
  - 说明世纪位或年份高位的处理方式。

  **验收标准**：写入后读回的数据与目标时间一致，非法时间不会直接写入芯片。

  **本题编写位置**：新建 `src/Labs/RtcSetLab/RtcSetLab.c` 和 `.h`；在此完成十进制校验、BCD 转换与写后读回验证，底层 I2C 操作仍调用 `IIC.c` 的公共接口。

- [ ] **题目 28：RTC 闹钟与外部中断**（提高）

  **目标**：练习 I2C 配置、RTC 闹钟输出、外部中断和业务标志位。

  **要求**：

  - 通过 I2C 设置闹钟时间。
  - 配置 RTC 闹钟中断输出和单片机外部中断输入。
  - ISR 中只设置 `alarm_event` 标志，不直接执行复杂 I2C 或串口操作。
  - 主循环检测到 `alarm_event` 后，执行 LED、蜂鸣器和串口报警。
  - 处理闹钟标志清除，避免中断重复触发。
  - 增加 KEY2 手动停止报警功能。

  **验收标准**：到达闹钟时间后能够触发一次业务报警，按键停止后不会立刻再次响起。

  **本题编写位置**：新建 `src/Labs/RtcAlarmLab/RtcAlarmLab.c` 和 `.h`；RTC 配置、外部中断事件标志与报警业务流程写在此 Lab，ISR 所在驱动文件只设置标志位。

## 第九阶段：综合练习

- [ ] **题目 29：非阻塞倒计时器**（综合）

  **目标**：综合使用按键、定时器、数码管、LED 和蜂鸣器。

  **要求**：

  - KEY1 开始 10 秒倒计时，KEY2 暂停或继续，KEY3 复位。
  - 使用 `system_ms` 每 1000 ms 减少 1 秒。
  - 数码管显示剩余秒数。
  - 倒计时过程中 LED 按固定节奏闪烁。
  - 倒计时到 0 后蜂鸣器响 3 次，并保持 LED 状态明确。
  - 所有按键都必须经过消抖，不能使用长时间阻塞延时。

  **验收标准**：倒计时过程中按键仍能响应，暂停后数字不减少，复位后从 10 秒重新开始。

  **本题编写位置**：新建 `src/Labs/CountdownLab/CountdownLab.c` 和 `.h`；在该 Lab 保存倒计时状态、按键事件和各时间变量，调用 Timers、Nixie、LED、Buzzer 接口。

- [ ] **题目 30：多任务调度器**（综合）

  **目标**：把多个独立的周期任务组织到一个主循环中。

  **要求**：

  - 至少同时运行 LED 300 ms 任务、蜂鸣器任务、按键扫描任务、ADC 采样任务和串口处理任务。
  - 每个任务都使用独立的时间变量或独立的任务结构体。
  - 每个任务执行时间尽量短，不能在任务内部使用长时间 `delay`。
  - 一个任务发生异常时，不能阻塞其他任务。
  - 记录每个任务的执行周期、触发条件和使用的状态变量。
  - 检查多个任务是否会同时修改同一个外设，并建立清晰的所有权规则。

  **验收标准**：多个外设能够同时工作，按键响应正常，任一任务不会因为等待而拖住整个主循环。

  **本题编写位置**：新建 `src/Labs/SchedulerLab/SchedulerLab.c` 和 `.h`；把各项任务调度逻辑集中于此。已有 LED、Buzzer、ADC、UART 模块不重写，只通过公开接口调用。

- [ ] **题目 31：综合环境报警器**（综合）

  **目标**：完成一个接近实际产品的小型应用。

  **要求**：

  - 使用 NTC 采集温度，ADC 采集电位器，RTC 提供时间。
  - 数码管显示当前温度或时间，KEY1 切换显示页面。
  - 温度超过阈值后 LED 闪烁、蜂鸣器间歇报警。
  - 电位器调整报警蜂鸣器音量对应的 PWM 占空比，或调整马达速度。
  - UART 支持读取当前状态，并支持修改报警阈值。
  - KEY2 能够暂时静音，但不能关闭温度检测。
  - 所有功能都必须采用模块接口和非阻塞任务方式实现。

  **验收标准**：温度、时间、按键、串口和报警功能能够同时工作；代码中能清楚区分驱动层、设备层和业务层。

  **本题编写位置**：新建 `src/Labs/EnvironmentAlarmLab/EnvironmentAlarmLab.c` 和 `.h`；这是综合业务层，组合已有 NTC、ADC、RTC、Nixie、LED、Buzzer、UART 模块，不把各模块的寄存器代码复制进来。

- [ ] **题目 32：整理并重构一个完整实验模块**（综合）

  **目标**：把前面练习中的代码整理成可维护的工程结构。

  **要求**：

  - 为实验模块建立独立的 `.h` 和 `.c` 文件。
  - 头文件只暴露必要的公开函数、类型和宏。
  - 模块内部状态使用 `static`，不直接暴露全局变量。
  - 初始化函数只负责初始化，任务函数只负责周期推进，停止函数负责清理状态。
  - 所有跨模块调用都通过头文件声明。
  - 为状态变量、时间变量、输入输出有效电平和边界条件添加注释。
  - 在 Keil 工程中加入所有新源文件，并完成一次干净构建。
  - 编写一份实验说明，记录接线、操作步骤、预期现象和已知限制。

  **验收标准**：删除或注释掉其他实验后，本实验仍能独立运行；重新打开工程时能够快速理解各模块职责。

  **本题编写位置**：选择一个已完成的综合 Lab（建议第 31 题的 `EnvironmentAlarmLab`）进行整理，不新建第二套功能代码；补全其 `.c/.h` 注释，并在 `src/Labs/EnvironmentAlarmLab/实验说明.md` 编写实验说明。

## 每道题完成后的记录模板

完成题目后，可以在题目下方补充以下内容：

```text
完成日期：
实际修改文件：
实际现象：
遇到的问题：
问题原因：
最终解决方式：
本题新掌握的知识：
还不理解的地方：
```

## 推荐练习顺序

```text
01~05  GPIO、LED、按键和消抖
06~09  蜂鸣器与简单外设配合
10~13  定时器、中断和 system_ms
14~16  非阻塞状态机
17~19  PWM、ADC 和马达
20~22  UART 和命令控制
23~25  ADC、NTC、数码管和报警
26~28  I2C、RTC 和外部中断
29~32  多任务综合项目
```

当前学习重点建议放在 `10 → 11 → 12 → 14 → 15 → 05`，先把“系统时间、时间差、状态机、非阻塞按键”这条主线练熟，再扩展到更多外设。
